import { Activity, BarChart3, Database, RotateCcw } from 'lucide-react'
export default function RenderMonitor({ stats, events }) { return <aside className="monitor"><div className="monitor-title"><div><p className="eyebrow">TELEMETRY</p><h2>Render monitor</h2></div><Activity size={20} className="cyan-icon" /></div><div className="stat-grid"><div><BarChart3 size={17} /><strong>{stats.total}</strong><small>Total renders</small></div><div><RotateCcw size={17} /><strong>{stats.cards}</strong><small>Cards rendered</small></div><div><Database size={17} /><strong>{stats.hits}</strong><small>Memo cache hits</small></div><div><Activity size={17} /><strong>{stats.callbacks}</strong><small>Callback recreations</small></div></div><div className="monitor-section"><div className="monitor-label"><span>EVENT RENDER LOAD</span><span>live</span></div>{events.map((event) => <div className="bar-row" key={event.id}><div><span>{event.title}</span><small>{event.renders || 0} renders</small></div><div className="bar-track"><i style={{ width: `${Math.min(100, ((event.renders || 0) / Math.max(5, stats.maxEventRenders)) * 100)}%` }} /></div></div>)}</div><p className="monitor-footnote">Toggle optimizations above to compare component work in real time.</p></aside> }
