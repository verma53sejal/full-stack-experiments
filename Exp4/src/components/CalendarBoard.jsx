import DayColumn from './DayColumn'
import CategoryFilter from './CategoryFilter'
export default function CalendarBoard({ days, events, activeCategory, onCategoryChange, onDrop, onSelect, onDragStart, onRender, renderVersion }) { return <section className="calendar-section"><div className="section-heading calendar-heading"><div><p className="eyebrow">SEPTEMBER 08 — 14, 2025</p><h2>Your week, in motion</h2></div><CategoryFilter active={activeCategory} onChange={onCategoryChange} /></div><div className="calendar-grid">{days.map((day) => <DayColumn key={day.name} day={day.name} date={day.date} events={events.filter((event) => event.day === day.name && (activeCategory === 'All' || event.category === activeCategory))} onDrop={onDrop} onSelect={onSelect} onDragStart={onDragStart} onRender={onRender} renderVersion={renderVersion} />)}</div></section> }
