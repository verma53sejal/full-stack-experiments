import { X } from 'lucide-react'
export default function EventModal({ event, onClose, onEdit }) { if (!event) return null; return <div className="modal-backdrop" onClick={onClose}><div className="event-modal" onClick={(e) => e.stopPropagation()}><button className="modal-close" onClick={onClose} aria-label="Close"><X size={19} /></button><span className={`category-badge modal-badge category-${event.category.toLowerCase()}`}>{event.category}</span><p className="eyebrow">EVENT DETAILS</p><h2>{event.title}</h2><div className="detail-grid"><span>DAY<strong>{event.day}</strong></span><span>TIME<strong>{event.time}</strong></span><span>CATEGORY<strong>{event.category}</strong></span></div><p className="modal-description">{event.description}</p><div className="modal-actions"><button className="secondary-button" onClick={onClose}>Close</button><button className="primary-button" onClick={onEdit}>Edit event</button></div></div></div> }
