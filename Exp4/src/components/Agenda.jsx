import { ArrowUpRight, ListTodo } from 'lucide-react'
export default function Agenda({ events }) { return <section className="agenda"><div className="section-heading"><div><p className="eyebrow">TODAY, MON 08</p><h2>Live agenda</h2></div><ListTodo size={20} className="muted-icon" /></div><div className="agenda-list">{events.map((event) => <div className="agenda-item" key={event.id}><time>{event.time}</time><span className={`agenda-dot dot-${event.category.toLowerCase()}`} /><div><strong>{event.title}</strong><small>{event.category} · {event.description}</small></div><ArrowUpRight size={16} /></div>)}</div></section> }
